import React from 'react'
import { getCountry, getFromCode } from '@/lib/data'

export default async function Borders(country: string) {

    // const data = await Promise.all(
    //     borders.map(async (border: string) => {
    //         const result = await getFromCode(border)
    //         return result.name.common
    //     })
    // )

    // console.log(data);


    return (
        <div>
            test
        </div>
    )
}
